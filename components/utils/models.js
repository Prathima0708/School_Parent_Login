class Category {
  constructor(id, title, color, icon,text) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.icon = icon;
    this.text=text;
  }
}

export default Category;
