class Category {
  constructor(id, title, color, icon, txtclr) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.icon = icon;
    this.txtclr = txtclr;
  }
}

export default Category;
