class Category {
  constructor(id, title, color, icon, txtclr, subTitle) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.icon = icon;
    this.txtclr = txtclr;
    this.subTitle = subTitle;
  }
}

export default Category;
